import { Color } from 'cc';

/**
 * Единый источник правды по палитре и балансу плейбла.
 * Меняем здесь — весь визуал/геймплей подхватывает.
 */

// --- Neon палитра фона ---
export const BG_TOP = new Color(26, 11, 61, 255);    // #1A0B3D центр
export const BG_BOTTOM = new Color(11, 11, 42, 255); // #0B0B2A край

// --- Цвета шаров (максимально различимы в неоне) ---
export const BUBBLE_COLORS: Color[] = [
    new Color(0, 240, 255, 255),   // cyan    #00F0FF
    new Color(255, 46, 159, 255),  // magenta #FF2E9F
    new Color(180, 255, 57, 255),  // lime    #B4FF39
    new Color(255, 158, 0, 255),   // amber   #FF9E00
    new Color(177, 75, 255, 255),  // violet  #B14BFF
];

// --- Цвет шара-бомбы (снаряд-«бомба», не участвует в матче по цвету) ---
export const BOMB_COLOR = new Color(255, 120, 30, 255); // #FF781E оранжево-красный

export const CFG = {
    // Геометрия
    bubbleRadius: 42,          // радиус шара, px (визуал = 2*radius; хитбокс тот же)
    glowScale: 1.25,           // размер glow-спрайта под телом (× диаметр шара)
    topMargin: 300,            // потолок поля от верха, px — кластеры спавнятся под шкалой
    bottomMargin: 200,         // высота зоны пуска снизу, px
    sideMargin: 20,            // отступ от боковых стен, px
    sideFrame: 40,             // негеймплейная рамка слева/справа, px (поле уже на эту величину)

    // Снаряд
    projSpeed: 1500,           // скорость полёта, px/сек

    // Поле
    descendSpeed: 8,           // скорость опускания кластеров, px/сек
    spawnInterval: 2.0,        // интервал спавна новых кластеров, сек
    clusterMin: 4,             // мин. шаров в кучке
    clusterMax: 8,             // макс. шаров в кучке
    clusterPackMin: 1.5,       // мин. расстояние между шарами в кучке (× radius) — плотность
    clusterPackMax: 2.0,       // макс. расстояние между шарами в кучке (× radius)
    foreignGap: 2.0,           // мин. расстояние до шаров ДРУГИХ кучек (× radius) — против наложения
    initialRowGap: 3.4,        // вертикальный шаг между стартовыми кучками (× radius)
    maxBubbles: 40,            // предохранитель по кол-ву шаров на поле
    initialClusters: 3,        // сколько кучек на старте

    // Матч / комбо
    contactFactor: 1.25,       // множитель суммы радиусов для «связности» кластера
    chainRadius: 164,          // радиус цепной реакции по тому же цвету, px
    chainPopDelay: 0.06,       // задержка между лопаньем шаров кластера, сек (эффект цепочки)

    // Очки
    scorePerBubbleMin: 100,    // очки за один лопнутый шар (низ диапазона)
    scorePerBubbleMax: 200,    // очки за один лопнутый шар (верх диапазона)
    targetScore: 9000,         // цель победы

    // Juice-тайминги, сек
    hitstopSmall: 0.04,
    hitstopBig: 0.09,
    hitstopComboThreshold: 3,
    shockwaveGrow: 0.35,
    popupRise: 60,
    popupTime: 0.6,
    shakeTime: 0.25,
    shakeSmall: 4,
    shakeBig: 10,
    timeScaleSlow: 0.65,       // замедление на крупном комбо
    slowMoTime: 0.12,

    // --- Аудио ---
    musicVolume: 0.45,         // громкость фоновой музыки (loop)
    musicFadeIn: 1.2,          // fade-in музыки при старте, сек
    musicDuckOnWin: 0.4,       // до какой доли громкости приглушить музыку на победе
    sfxVolume: 0.9,            // базовая громкость поп-звука
    popPitchBase: 1.0,         // питч попа при combo=1
    popPitchStep: 0.12,        // прибавка питча за каждый уровень комбо
    popPitchMax: 2.2,          // потолок питча (Web Audio; иначе fallback громкостью)
    winVolume: 1.0,            // громкость джингла победы
    shootVolume: 0.7,          // громкость выстрела
    bombVolume: 1.0,           // громкость взрыва бомбы
    stickVolume: 0.8,          // громкость прилипания шара
    cascadeVolume: 1.0,        // громкость финального каскада

    // --- Juice пузырей ---
    spawnPopTime: 0.28,        // длительность pop-in при спавне, сек
    spawnPopOvershoot: 1.15,   // пиковый масштаб pop-in (backOut)
    idleBobAmp: 3,             // амплитуда покачивания статичных шаров, px
    idleBobTime: 1.6,          // период покачивания, сек
    trailInterval: 0.012,      // шаг между «призраками» трейла снаряда, сек
    trailFade: 0.22,           // время затухания призрака трейла, сек

    // --- Danger line (только драма, без проигрыша) ---
    dangerLineOffset: 175,     // высота danger-линии над пушкой (от launcherY вверх), px
    dangerShakeEvery: 0.55,    // как часто «дёргать» экран, пока шары за линией, сек
    dangerShakeMag: 5,         // амплитуда тряски в опасности, px

    // --- Бомба (снаряд игроку) ---
    bombChance: 0.16,          // шанс, что следующий снаряд — бомба (0..1)
    bombRadius: 220,           // радиус взрыва бомбы, px
    bombScoreMult: 1.5,        // множитель очков за шар, снесённый бомбой
    bombFps: 24,               // скорость покадровой анимации бомбы, кадров/сек
    bombScaleX: 1.0,           // масштаб бомбы по ширине (× диаметр шара)
    bombScaleY: 1.18,          // масштаб бомбы по высоте (× диаметр шара) — вытянуть вверх

    // --- Очередь следующих снарядов (превью) ---
    queueCount: 3,             // сколько следующих снарядов показывать
    previewX0: 165,            // X первого превью (правее пушки), px
    previewGap: 82,            // шаг между превью по X, px
    previewScale: 0.72,        // масштаб превью-шара (× bubbleRadius)

    // --- Idle заряженного шара ---
    launcherScale: 1.15,       // масштаб заряженного шара в пушке (× bubbleRadius, визуально крупнее)
    launcherPulse: 0.05,       // амплитуда «дыхания» заряженного шара (доля масштаба)
    launcherPulseTime: 0.9,    // период «дыхания», сек

    // --- Пекшот (максимум сока) ---
    pkLogoDrop: 120,           // с какой высоты лого «падает» вниз при входе, px
    pkStagger: 0.12,           // шаг каскадного появления элементов, сек
    pkRainCount: 26,           // сколько неон-пузырей в фоновом «дожде»
    pkBurstCount: 42,          // сколько пузырей в стартовом залпе (снизу вверх)
    pkBurstRMin: 26,           // мин. радиус пузыря залпа, px
    pkBurstRMax: 52,           // макс. радиус пузыря залпа, px
    pkBurstSpread: 1.4,        // разброс старта пузырей по времени, сек (растягиваем пик частиц)
    pkPopShards: 5,            // сколько осколков при лопке пузыря (Graphics-ноды — дорогие)
    pkCtaDelay: 1.25,          // задержка 2-го глитча + появления CTA после показа пекшота, сек
    pkShineTime: 1.6,          // период gleam-вспышки по кнопке, сек
};
